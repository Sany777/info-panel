---
description: Never write comments in the code
---

The user has explicitly requested to NEVER write comments in the code.
When generating, refactoring, or modifying code, do NOT include comments (e.g. `//` or `/* */`).
Code should be self-documenting.
